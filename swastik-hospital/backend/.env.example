# MongoDB Atlas (replace with your cluster URL)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/swastik_hospital
DB_NAME=swastik_hospital

# JWT
SECRET_KEY=supersecretkey
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Optional: allow frontend origin
# CORS_ORIGINS=http://localhost:3000
